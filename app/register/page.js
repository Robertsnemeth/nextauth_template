import RegisterWidget from '../components/Register/RegisterWidget'
import { HomeButton } from '../components/buttons'

const Register = () => {
  return (
        <main className='flex flex-col items-center gap-3 p-10 min-h-screen'>
          <RegisterWidget/>
        </main>
        )
}

export default Register